# Optional: IBM Cloud Kubernetes / OpenShift
# (kept as reference — not used in current Docker Compose deployment)

# k8s/ contains Kubernetes manifests
# terraform/ contains IaC for cluster setup
# These are NOT required for the Docker Compose on VM deployment
